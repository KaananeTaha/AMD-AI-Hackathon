# Agent (the only dependency that ships in the Docker image)
openai>=1.30.0

# Nova desktop UI (local dev only — never installed in the image)
PySide6>=6.6.0
markdown>=3.5
Pygments>=2.17
requests>=2.31
